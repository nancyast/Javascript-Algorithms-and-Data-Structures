// Count Unique Values
// Implement a function called countUniqueValues, which accepts a sorted array
// and counts a unique values in the array. There can be negative numbers in the array,
// but it will always be sorted.

// Example:
// countUniqueValues([1, 1, 1, 1, 1, 2]) should return 2
// countUniqueValues([1, 2, 3, 4, 4, 5, 6, 8, 8, 9]) should return 8
// countUniqueValues([]) should return 0
// countUniqueValues([-2, -1, 0, 1]) should return 4
